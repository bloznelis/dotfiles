if [ -z "$DISPLAY" ] && [ "$XDG_VTNR" = 1 ]; then
  xset r rate 300 40
  exec startx
fi

export PATH="$PATH:/home/lukas/.cache/scalacli/local-repo/bin/scala-cli"

# >>> coursier install directory >>>
export PATH="$PATH:/home/lukas/.local/share/coursier/bin"
# <<< coursier install directory <<<
